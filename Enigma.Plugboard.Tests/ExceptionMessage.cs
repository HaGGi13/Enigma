namespace Enigma.Plugboard.Tests
{
    internal static class ExceptionMessage
    {
        internal const string WiredPairsMustBePairsExceptionMessage = "Wired pairs must be pairs. Every letter must have another one.";
        internal const string WrongLengthExceptionMessage = "A wired pair string must be less or equal 10.";
        internal const string ContainsNonLetterExceptionMessage = "Wired pairs must consist of letters only.";
    }
}
